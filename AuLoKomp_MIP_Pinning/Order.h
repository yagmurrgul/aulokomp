#pragma once
#ifndef ORDER_H
#define ORDER_H

#include <iostream>
#include <vector>
#include "item.h"
#include <map>

class Order {
	/*
public:

	void order(std::vector<Item> items);

	void order(std::map<Item, int> items);

private:	

	std::map<Item, int> _items;	
	*/
};
#endif
